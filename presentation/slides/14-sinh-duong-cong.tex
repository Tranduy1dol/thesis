\begin{frame}{Cải tiến thuật toán Cocks-Pinch}
  \begin{table}
    \small
    \begin{tabular}{p{3.7cm}p{4.8cm}p{3.8cm}}
      \toprule
      \textbf{Cải tiến} & \textbf{Lý do} & \textbf{Đánh đổi} \\
      \midrule
      $T \equiv 0 \pmod{2^{12}}$\newline$\Rightarrow \nu_2(r-1) \geq 36$
        & Trường $\mathbb{F}_r$ hỗ trợ NTT\newline(cần cho ZK proof, MSM)
        & Thu hẹp không gian\newline tìm kiếm $T$ \\[8pt]
      Mở rộng lưới nâng\newline$\Rightarrow \nu_2(p-1) \geq 36$
        & Trường $\mathbb{F}_p$ hỗ trợ NTT\newline(kháng TNFS, tăng tốc pairing)
        & Số lần thử tăng\newline$\sim$10$\times$ (3000--6000) \\[8pt]
      Kích thước $p = 1024$ bit\newline($r = 512$ bit)
        & Bảo mật 256-bit đối xứng\newline(vượt NIST 128-bit)
        & Phép nhân trường\newline chậm hơn ECC 256-bit \\
      \bottomrule
    \end{tabular}
  \end{table}
  \vspace{0.3em}
  \textbf{Kết quả:} Ba cải tiến kết hợp $\Rightarrow$ đường cong KSS18 \textbf{đầu tiên} có
  \textbf{cả} $\mathbb{F}_r$ \textbf{và} $\mathbb{F}_p$ hỗ trợ NTT ---
  ưu thế mà BLS12-381 \textbf{không có} ở $\mathbb{F}_p$.
\end{frame}
