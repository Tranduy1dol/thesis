\begin{frame}{Cơ sở toán học: Đường cong Elliptic}
  \begin{block}{Short Weierstrass trên $\mathbb{F}_p$}
    $E(\mathbb{F}_p) = \{(x,y)\in\mathbb{F}_p^2 \mid y^2 = x^3 + ax + b\} \cup \{\mathcal{O}\}$
  \end{block}
  \begin{columns}[t]
    \column{0.5\textwidth}
      \textbf{Phép cộng điểm} $P_1 + P_2 = P_3$:
      \[
        \lambda = \frac{y_2 - y_1}{x_2 - x_1}, \quad
        x_3 = \lambda^2 - x_1 - x_2
      \]
      \textbf{Phép nhân vô hướng}:\\
      $[k]P$ bằng thuật toán \emph{Double-and-Add},\\
      độ phức tạp $O(\log k)$
    \column{0.5\textwidth}
      \begin{block}{Bài toán logarit rời rạc (ECDLP)}
        \begin{itemize}
          \item Cho $P, Q = [k]P$, tìm $k$ là\\bài toán khó (cơ sở bảo mật)
          \item Độ phức tạp: $O(\sqrt{r})$ với\\thuật toán Pollard's rho
          \item Nhóm con cấp $r$ nguyên tố\\đảm bảo an toàn
        \end{itemize}
      \end{block}
  \end{columns}
\end{frame}
